tasks:
  - init: npm install --prefix servidor
    command: npm start --prefix servidor
  - name: servidor
    init: npm install
    command: npm start

ports:
  - port: 3000
    onOpen: open-browser

vscode:
  extensions:
    - dbaeumer.vscode-eslint